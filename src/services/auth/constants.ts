export const BEARER_HEADER = 'WWW-Authenticate'
export const BEARER_HEADER_INVALID = `Bearer realm='sign', error="invalid_request"`
export const BEARER_HEADER_EXPIRED = `Bearer realm='sign', error="invalid_token"`
